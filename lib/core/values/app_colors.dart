import 'package:flutter/material.dart';

class AppColors {
  // Existing colors
  static const primary = Colors.blue;
  static const secondary = Colors.blueAccent;
  static const background = Colors.white;
  static const text = Colors.black87;
  static const error = Colors.redAccent;

  // New colors for Connect Screen
  static const purpleDark = Color(0xFF2E0D46);
  static const purpleLight = Color(0xFF5A1C80);
  static const purpleAccent = Color(0xFF8A2BE2);
  static const cardBackground = Color(0xFF2A1B3D); // Dark semi-transparent
  static const textWhite = Colors.white;
  static const textGrey = Colors.white70;
  static const circleGlow = Color(0xFF4A148C);
}
